{
  "name": "1000ch.net",
  "private": true,
  "description": "Happens on me, Happens from me.",
  "license": "MIT",
  "repository": "1000ch/1000ch.net",
  "author": {
    "name": "1000ch",
    "email": "shogo.sensui@gmail.com",
    "web": "http://github.com/1000ch"
  },
  "bugs": {
    "url": "https://github.com/1000ch/1000ch.net/issues"
  },
  "config": {
    "postcss": "--use postcss-import --use autoprefixer --use cssnano"
  },
  "scripts": {
    "start": "npm run build && npm run css -- --watch & hugo server",
    "lint": "stylelint static/css/*.css",
    "css": "postcss $npm_package_config_postcss --output public/css/app.css static/css/app.css",
    "build": "npm run css"
  },
  "dependencies": {
    "normalize.css": "^7.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^7.1.0",
    "cssnano": "^3.10.0",
    "postcss-cli": "^4.0.0",
    "postcss-import": "^10.0.0",
    "stylelint": "^7.10.1",
    "stylelint-config-standard": "^16.0.0"
  }
}
