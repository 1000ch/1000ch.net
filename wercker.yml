box: wercker/nodejs
build:
  steps:
    - arjen/hugo-build:
        version: 0.12
    - npm-install
deploy:
  steps:
    - script:
        name: Create CNAME
        code: echo '1000ch.net' > _public/CNAME
    - lukevivier/gh-pages@0.2.1:
        token: $GITHUB_TOKEN
        basedir: _public