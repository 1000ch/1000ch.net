box: wercker/nodejs
build:
  steps:
    - arjen/hugo-build:
        version: 0.12
    - npm-install
deploy:
  steps:
    - script:
        name: Copy RSS
        code: cp _public/index.xml _public/rss.xml
    - lukevivier/gh-pages@0.2.1:
        token: $GITHUB_TOKEN
        basedir: _public